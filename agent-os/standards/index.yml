version: "1.0"
generated: 2026-03-08T12:00:00+00:00
project: Gantry

docker:
  compose-conventions:
    description: Service shape, healthcheck requirements, env_file secrets, named volumes, and depends_on patterns
  network-topology:
    description: Three-network topology (frontend/backend/monitoring), declaration in core, external reference in other groups

secrets:
  management:
    description: init-docker-secrets.sh workflow, idempotent yq pattern, docker-secrets.env lifecycle
